title: Stories
tabs:
  content:
    columns:
      - width: 1/1
        sections:
          # meta:
          #   type: fields
          #   fields:
          #     intro:
          #       type: textarea

          items:
            headline: Stories
            type: pages
            template: story
            empty: No items
            layout: cards
            image:
              back: mintcream
              ratio: 4/3
              query: page.files.findBy("name", "cover")
              # cover: true

  seo: seo/page
